# Deep Learning Diagnostic Toolkit

##  Installation

### Using pip with requirements.txt

```bash
python -m venv venv
# Activate virtual environment

# If the OS is Windows,run this:
venv\Scripts\activate

# Otherwise,namely macOS/Linux:
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt
```
The aim of this project is to show my humble viewpoint about the potential problem in this course.

Conda is not necessary if your OS is Mac.

The package in the requirement is in minimum possible,
namely without any package you would not run the main.py normally and errors will occur then!
It is OK to run with the latest python.

Still need more test since it is very beginning...

